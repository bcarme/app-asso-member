{% extends 'layoutEmail.html.twig' %}
{% set page = 'send_mail' %}

{% block body %}
    <div>
        <h2>Voici les nouvelles informations concernant le cours déplacé :</h2>
        <h3>Cours : {{ booking.title}}</h3>
        <h3>Date : {{ booking.beginAt|date('d/m/Y H:i') }}</h3>
        <h3>Lieu: {{ booking.location.place}}, {{ booking.location.address}}, {{ booking.location.zipcode}} {{ booking.location.city}}</h3>
        <p class="mt-3">Si vous souhaitez annuler, merci de vous connecter à <a href="{{ url('app_login') }}">votre espace</a> et cliquer sur Mes créneaux</p>
    </div>
{% endblock %}