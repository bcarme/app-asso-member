{% extends 'layoutEmail.html.twig' %}
{% set page = 'send_mail' %}

{% block body %}
    <div>
        <h2>Le cours suivant a été annulé :</h2>
        <h3>Cours : {{ booking.title}}</h3>
        <h3>Date : {{ booking.beginAt|date('d/m/Y H:i') }}</h3>
        <h3>Lieu: {{ booking.location.place}}</h3>
        <p class="mt-3">Si vous souhaitez réserver d'autres créneaux, merci de vous connecter à <a href="{{ url('app_login') }}">votre espace</a> et cliquer dans le Planning du Club</p>
    </div>
{% endblock %}